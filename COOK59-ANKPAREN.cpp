#include<bits/stdc++.h>
using namespace std;
int main()
{
	int t;
	cin>>t;
	while(t>0)
	{
		
		string str;
		getline(cin, str);
		int len = str.length();
		for(i=0; i<len; i++)
		{
			if(str[i]=="(")
			{
				str[i]==0;
			}
			else
			{
				str[i]==1;
			}
		}
		int k;
		cin>>k;
		
		t--;
	}
	return 0;
}
